[project]
name = "todo-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bottle>=0.13.4",
    "pytest>=8.4.2",
    "requests>=2.32.5",
    "webtest>=3.0.6",
]
